# クラスファイル（Javaコンパイル結果）
*.class

# jar、warなどのパッケージ成果物
# 忽略编译生成的 jar（比如 target/、out/ 中的）
/target/*.jar
/out/*.jar


# 但保留 WEB-INF/lib 下的 jar 包（重点）
!webapp/WEB-INF/lib/
!webapp/WEB-INF/lib/*.jar


*.war
*.ear

# Maven関連のビルド成果物（targetフォルダなど）
/target/
/dependency-reduced-pom.xml
/maven-status/

# Gradleを使用している場合のビルド成果物
.gradle/
/build/

# ログファイルや一時ファイル
*.log
*.tmp
*.bak

# OS依存の不要ファイル
.DS_Store
Thumbs.db

# Tomcatなどのサーバ実行時に生成される一時ファイル
/work/
/out/
/tomcat.*.tmp

# IDEA（IntelliJ）関連の設定（必要に応じて残す）
.idea/
*.iws
*.iml

# アップロードされた画像などを無視（ただしフォルダ自体は残す）
webapp/images/*
!webapp/images/.keep

# バックアップ目的以外のバージョン管理対象外ファイル（任意）
*.bak